
git checkout execution
git merge develop
git add .
git commit -m " "
git push 

Greedy baby push:

top ; SIGQUIT


signal(SIGQUIT, SIG_DFL);
signal(SIGINT, SIG_DFL);

signal(SIGQUIT, SIG_IGN);
signal(SIGINT, SIG_IGN);

disable_signals();
signal(SIGINT, SIG_DFL);
signal(SIGQUIT, SIG_IGN);



tcgetattr

tcsetattr
cat Makefile |>out3 cat (minishell: synthax error near unexpected token `>')

bash-3.2$ cat
^\Quit: 3

export ZZZ=
export ZZZ  // overwrites the variable (and it should not) Done!

<< xx cat
> hello
> 
> 
> 
> 
> 
> xx
hello
 // this also needs to print the empty lines

heredoc: ctrl-D doesn't work, ctrl-C doesn't work

#include <errno.h>
strerror(errno);

printf("minishell: synthax error near unexpected token `%s'\n", quote);
