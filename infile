
git checkout execution
git merge develop
git add .
git commit -m " "
git push 

Greedy baby push:

top ; SIGQUIT


signal(SIGQUIT, SIG_DFL);
signal(SIGINT, SIG_DFL);

signal(SIGQUIT, SIG_IGN);
signal(SIGINT, SIG_IGN);

disable_signals();
signal(SIGINT, SIG_DFL);
signal(SIGQUIT, SIG_IGN);


minishell: synthax error near unexpected token `END'
minishell$ echo $?
0
minishell$ hello > $ffff
minishell: ambiguos redirect
minishell$ hello > > hello // when synthax error there is no history recorded

minishell: synthax error near unexpected token >
minishell$ hello > > hello
minishell: synthax error near unexpected token >
minishell$ hello > 


